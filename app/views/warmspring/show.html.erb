<p id="notice"><%= notice %></p>
<div class="child row">
  <div class="wrapper col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <div class="blog-text">
      <p class="left"><strong>タイトル</strong></p>
      <p class="left"><%= @warmspring.title %></p>
      <p class="left"><strong>内容</strong></p>
      <p class="left"><%= @warmspring.content %></p>
      <p class="right">
        <strong>作成時間:</strong>
        <%= @warmspring.created_at.strftime("%y/%m/%d %p %l:%M") %>
      </p>
    </div>
    <p>コメント一覧</p>
    <div id="comments_area">
      <%= render partial: 'warmspring_comments/index', locals: { comments: @comments, warmspring: @warmspring } %>
    </div>
    <%= render partial: 'warmspring_comments/form', locals: { comment: @comment, waremspring: @warmspring } %>
    <% if current_user.id == @warmspring.user_id %>
      <%= link_to '編集', edit_warmspring_path(@warmspring) %> |
    <% end %>
    <%= link_to '戻る', warmspring_index_path %>
  </div>
</div>