<p id="notice"><%= notice %></p>
<div class="child row">
  <div class="wrapper col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <div class="blog-text">
      <p class="left"><strong>タイトル</strong></p>
      <p class="left"><%= @autumn.title %></p>
      <p class="left"><strong>内容</strong></p>
      <p class="left"><%= @autumn.content %></p>
      <p class="right">
        <strong>作成時間:</strong>
        <%= @autumn.created_at.strftime("%y/%m/%d %p %l:%M") %>
      </p>
    </div>
    <p>コメント一覧</p>
    <div id="comments_area">
      <%= render partial: 'autumn_comments/index', locals: { comments: @comments, autumn: @autumn } %>
    </div>
    <%= render partial: 'autumn_comments/form', locals: { comment: @comment, autumn: @autumn } %>
    <% if current_user.id == @autumn.user_id %>
      <%= link_to '編集', edit_autumn_path(@autumn) %> |
    <% end %>
    <%= link_to '戻る', autumn_index_path %>
  </div>
</div>