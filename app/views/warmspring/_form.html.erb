<p id="notice"><%= notice %></p>
<%= form_for(@warmspring, url: choose_new_or_edit_warmspring(warmspring)) do |f| %>
  <% if @warmspring.errors.any? %>
    <div id="error_explanation">
      <h2><%= @warmspring.errors.count %>件のエラーがあります。</h2>
      <ul>
        <% @warmspring.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.label :タイトル %>
  <%= f.text_field :title, class: 'form-control' %>
  <br>
  <div class="field-blog">
    <%= f.file_field :image %>
    <%= f.label :説明 %>
    <%= f.text_field :content, class: 'form-control' %>
    <br>
    <%= f.submit '投稿', class: 'btn btn-primary btn-block' %>
  </div>
<% end %>