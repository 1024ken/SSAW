<p id="notice"><%= notice %></p>
<%= form_for(@autumn, url: choose_new_or_edit_autumn) do |f| %>
  <% if @autumn.errors.any? %>
    <div id="error_explanation">
      <h2><%= @autumn.errors.count %>件のエラーがあります。</h2>
      <ul>
        <% @autumn.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <br>
  <%= f.submit %>
<% end %>
<%= link_to "ブログ一覧画面にもどる", autumn_index_path %>