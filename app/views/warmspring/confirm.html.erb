<div class="container">
  <div class="col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <h3>以下の内容で、送信する。</h3>
    <%= form_for(@warmspring, url: warmspring_index_path, method: 'post') do |f| %>
      <%= f.hidden_field :title %>
      <%= f.hidden_field :content %>
      <p>タイトル:<%= @warmspring.title %></p>
      <p>本文:<%= @warmspring.content %></p>
      <%= f.submit "登録する" %>
    <% end %>
    <%= form_for(@warmspring, url: new_warmspring_path, method: 'get') do |f| %>
      <%= f.hidden_field :title %>
      <%= f.hidden_field :content %>
      <%= f.submit "もどる", name: 'back' %>
    <% end %>
  </div>
</div>